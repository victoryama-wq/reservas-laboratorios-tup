{
  "name": "reserva-de-laboratorios",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test:functions": "npm --prefix functions test",
    "test:web": "npm --prefix apps/web test -- --watch=false",
    "build:functions": "npm --prefix functions run build",
    "build:web": "npm --prefix apps/web run build",
    "lint:functions": "npm --prefix functions run lint",
    "validate": "npm run test:functions && npm run test:web && npm run lint:functions && npm run build:functions && npm run build:web",
    "test": "npm run test:functions && npm run test:web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@angular/cli": "^21.2.11",
    "firebase-tools": "^15.18.0"
  },
  "dependencies": {
    "firebase": "^12.13.0"
  }
}
